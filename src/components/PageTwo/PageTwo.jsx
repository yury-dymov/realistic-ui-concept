import React from 'react';
import PageHeader from 'react-bootstrap/lib/PageHeader';

/* eslint-disable max-len */

function PageTwo() {
  return (
    <div className="readme-text">
      <PageHeader>Some Random Page</PageHeader>
      <p>
        Random page, where user navigated to.
      </p>
      <p>
        Check that running requests are keep running, and you are also capable of navigating back to the relevant page easily if desired or instantly replaying the operation in case of failure by using dropdown menus in the top right of the navigation bar.
      </p>
    </div>
  );
}

export default PageTwo;

/* eslint-enable max-len */
